dist: trusty
sudo: false
language: cpp

addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - g++-6

script:
  - mkdir ./bin
  - g++-6 ./test/*.cpp -Wall -Wextra -DDEBUG -std=c++11 -I./include -o ./bin/rua_tests_d
  - ./bin/rua_tests_d
